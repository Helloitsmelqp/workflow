name: Workflow

on:
  pull_request:
    branches: ["main"]

jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: run test
        run: echo "CI"
